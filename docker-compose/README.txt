### 使用方法
第一种：
   1， 将docker-compose 单独复制到独立目录中。
   2， 将rulemanager 整个目录复制到 docker-compose/data/rulemanager/server/下。
   3， 修改 docker-compose/data/rulemanager/config/env 配置文件。
   4， 进入 docker-compose 目录，执行docker-compose up -d 即可运行。（镜像已上传到docker hub 中）。

